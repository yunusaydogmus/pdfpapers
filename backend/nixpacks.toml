# ════════════════════════════════════════════
# Nixpacks — configuration Railway minimale
# LibreOffice et Tesseract ajoutés après
# validation du premier déploiement
# ════════════════════════════════════════════

[phases.install]
cmds = ["npm ci --production"]

[start]
cmd = "node server.js"
